Do not add unnecessary comments to the codebase.

Use proper formatting with clear whitespace between logical sections; avoid dense, compressed code.

Use Hugeicons through individual icon imports such as `@hugeicons/core-free-icons/Add01Icon` and the shared AppIcon renderer. Avoid root, wildcard, and dynamic catalog imports so only needed icons load in development and production.
